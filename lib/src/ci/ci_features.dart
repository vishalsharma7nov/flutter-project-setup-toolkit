/// Feature flags for CI Studio UI and detect surfaces.
///
/// Backend act/Docker code remains; set [ciActStudioEnabled] to true to expose
/// "Run with act" and Docker in DevOps optional checks again.
const ciActStudioEnabled = false;
